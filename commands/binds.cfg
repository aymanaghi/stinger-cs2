// 🖱️ MOUSE BINDS — Jump on mouse wheel, easy comms
bind "MWHEELUP" "+jump"          // Jump on scroll up
bind "MWHEELDOWN" "buymenu"      // Buy menu on scroll down
bind "MOUSE3" "toggle cl_righthand 0 1"  // Toggle left/right hand view

// Quick comms (ping, decoy, etc.)
bind "v" "radio2"                // "Go go go"
bind "c" "radio1"                // "Affirmative"
bind "x" "radio3"                // "Enemy spotted"

// Crouch bind (consistent movement)
bind "CTRL" "+duck"
